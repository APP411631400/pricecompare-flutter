// lib/screens/recommend_page.dart
import 'package:flutter/material.dart';

class RecommendPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('商品推薦')),
      body: Center(child: Text('AI 推薦區塊（預留模型/後端）')),
    );
  }
}